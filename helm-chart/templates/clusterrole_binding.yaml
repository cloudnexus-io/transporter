apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "transporter.fullname" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "transporter.fullname" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "transporter.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
